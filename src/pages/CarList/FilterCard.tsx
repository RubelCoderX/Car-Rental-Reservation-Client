type TFilterCar = {
  price: number;
  setPrice: number;
  carType: string;
  setCarType: string;
};
const FilterCard = ({ price, setPrice, carType, setCarTyep }: TFilterCar) => {
  return (
    
  );
};

export default FilterCard;
